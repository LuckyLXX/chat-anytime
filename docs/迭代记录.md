# 桌面端需求（已完成）

* [X]  左侧边栏折叠 + hover 浮层（2026-08-25：用户要求侧栏可点击折叠成图标窄条、hover 浮出完整面板、折叠图标可点击直达，默认展示新增会话并在当前工作区新建。纯渲染端改动 App.tsx + styles.css + 主题契约同步：① 新增 sidebarCollapsed/sidebarFlyoutOpen 状态（不持久化，重启默认展开），抽取 sidebarInner 让展开态与 hover 浮层复用同一套侧栏内容（品牌区/文件视图/助手-话题列表/新建话题/底部署栏）；② 展开态品牌区右侧加折叠按钮（data-control="sidebar-collapse"，PanelLeftClose），折叠态渲染 sidebar-rail 窄条（48px，新增 --layout-sidebar-collapsed-width token）：新增会话（accent 突出，data-control="new-session"，createNewSession() 无参即在当前工作区新建）、话题（setSidebarView("topics")+setSidebarTab("topics")+openSidebarFlyout，避免停在文件视图时浮层显示文件树）、助手（同理切 agents）、底部长条展开按钮（PanelLeftOpen）；③ hover rail 临时浮出完整面板（复用 sidebarInner，rail 与浮层同为树内子元素避免鼠标跨移闪跳，mouseleave 150ms 防抖收起）；④ 0.18s 宽度平滑动画（desktop-shell grid-template-columns transition + sidebar width transition）；⑤ 主题契约同步——<html> 新增布尔状态 data-ui-sidebar-collapsed + 布局量 token --layout-sidebar-collapsed-width（AGENTS.md 主题 bullet + theme-guide.md）。折叠态图标集为「新增会话 + 话题 + 助手」，hover 浮出的完整面板内仍含文件/设置入口。纯 UI 可测逻辑极少，不新增测试；npm test 524 + build 全绿）
* [X]  计划模式：先出计划文档、批准后才实施（2026-08-25，feature/plan-mode：参考 deepseek-harness 的 plan-mode 成熟设计（会话级协作状态 + exit 审查门 + 模式不 enforce），对齐 Proma/PiDeck 的计划落盘习惯。能力簇 `runtime-plan-tools.ts` + `plan-store.ts`：① 状态——会话级开关持久化于 `chatanytime-sessions/<agentId>/plans/<sessionId>.json`（原子写、createSession 恢复），快照新增 `planMode`；入口三路：/plan 斜杠命令（可动态反映当前状态）、访问权限下拉新增「计划模式」项（data-control="plan-toggle"，独立于 accessMode 的协作轴，不写 settings）、模型自主 `enter_plan_mode` 工具（任务明显需要先规划时调用，描述级软约束）；② 注入——第四个内联扩展 pidesktop-plan-mode 在每次 LLM 调用前的 `context` 事件里把叙事追加到最后一条 user 消息文本尾部：进入时完整指引一次、之后每回合 agent_start 挂短提醒，只进请求不进 transcript/系统提示（todo/vision 同款缓存纪律，其余请求原样放行）；③ 审查——`exit_plan_mode` 工具（仅计划模式可用、空计划拒绝）以 `QuestionItem.detail`（协议新增可选 markdown 详情字段，向后兼容）把计划全文展示在提问面板题目与选项之间，单选「批准计划，开始实施/不批准，继续完善计划」+ 自定义输入即反馈；只有恰好选中批准才退出模式并返回实施指令（拒绝=保持模式+用户意见原样回传为 corrective isError；忽略=保持模式）；④ 批准落盘——主进程侧 `saveApprovedPlan` 写入 `<workspace>/docs/plans/YYYY-MM-DD-<标题>.md`（标题净化、同日同名加序号不覆盖、失败不阻塞批准），模型规划阶段零写操作。子代理不继承模式；模式不改权限轴、不滤工具（对齐 dsh 分离纪律）。AGENTS.md 计划模式条目 + 主题钩子契约（plan-toggle 进 AGENTS.md/theme-guide/check_theme KNOWN_CONTROLS/variables.md/SKILL.md）。新增 plan-store.test.ts 7 例 + runtime-plan-tools.test.ts 16 例 + QuestionPanel detail 渲染 2 例；npm test 516 + build 全绿。**同日 review 修复（二修）**：① 后台会话跨助手写盘错目录——setPlanMode 运行时用全局 currentAgent 算路径，parked 会话切助手后会把计划模式状态写进别的助手目录，改为按 record.agent.id 计算；② 删除会话/移除工作区不清理 plans/<sessionId>.json 孤儿文件——session.delete/workspace.remove 同步 unlink；③ 重试缺口——叙事只注入一次，注入的那次请求失败重试时模型全程无指引，新增 after_provider_response(status≥400) 兑底重挂短提醒；④ 审查面板 detail 误用 data-role="question-detail"（违反 user/assistant/extension 契约）已删除；⑤ 计划开关 aria 改为 menuitemcheckbox；⑥ demo 会话切换重置 planMode；⑦ AGENTS.md 顶部 bullet 三个扩展同步为四个。补 extension 捕获测试 5 例；npm test 523 + build 全绿。**同日三修（实测 bug）：计划模式审查「点批准却被判未批准」**——根因在渲染端 QuestionPanel 单选点选即提交的时序：toggleOption 里 setDrafts 是异步的，紧随其后的 advance() 用闭包里的**旧 drafts** 构造答案（单选 serializeAnswer = custom || selected.join，selected 尚未包含刚点的选项）→ 无自定义输入时提交空字符串 → 主进程 parsePlanReview([""]) 不等于批准选项 → 误判 rejected（无反馈）。这是 2026-08-20「单选点选即推进」改造引入的既有 bug，普通文本题未暴露。修复：单选点选提交改为基于本次点击的选项构造答案（新纯函数 singleSelectionAnswer(custom, option) 保留自定义优先语义，提取后可直接单测）；非末题跳页不受影响；审查文案补充「点选项即提交；如需修改请在输入框中写下意见」。npm test 524 + build 全绿）

* [X]  马卡龙多巴胺乐园 candy-dopamine 主题（2026-08-24：用户要求多巴胺配色主题，可用立绘/装饰/背景，生图走 rolldek 技能；方向对齐后定为「马卡龙糖果系」治愈风 + 双动漫少女立绘。生图经 rolldek gpt-image-2：明/暗两张马卡龙壁纸、左右两立绘、糖果条纹砖；立绘透明背景经边界泛洪抠图（近白背景非真透明）转 webp，PIL 优化后全套约 290KB。themes/candy-dopamine/ 纯 CSS + 资产零脚本——明暗双模式全套语义 token（粉/薄荷/奶黄/香芋紫马卡龙），顶栏糖果条纹底边 + 自定义侧栏/输入栏玻璃、双立绘走 --pi-layer-* 声明层（暗色用 data-layer-kind 滤镜压暗；landing/对话两档用 :root:not([data-ui-chat-empty]) 级联）、输入栏奶油糖果玻璃与糖果圆钮、生成中呼吸光环。chk：check_theme 全绿；npm test 490 + build 全绿）
* [X]  回复底部面板改为「交付产物」：bash/MCP 等产出型工具生成的图片等文件也能进列表并右侧预览（2026-08-24：原「本次变更文件」只采集 edit/write 的入参路径（changedWorkspaceFile 只认这两个工具），生图类 skill（rolldek/sensenova 等）经 bash 把图片落盘到工作区、输出里打印路径，bash 不打文件标记 → 图片永远进不了列表；右侧预览本就支持图片（readWorkspaceFilePreview 返回 kind:image + base64 内联，IMAGE_PREVIEW_LIMIT_BYTES=20MB），缺的只是产物采集。落地：① 协议 `ToolExecution` 新增 `changedFiles` 产物数组（可多个，保留 `changedFile` 单文件向后兼容，session-history 恢复同步）；② `workspace-preview.ts` 新增纯函数 `changedWorkspaceFiles`（edit/write 数组形态）/ `isArtifactProducingTool`（排除 read/grep/find/ls/cat 等纯读类工具防误报）/ `artifactCandidatesFromOutput`（从工具结果文本正则提取形如路径的 token → 安全相对化 → 排除忽略目录与非常见产物扩种名，去重限 16 个）+ `existingWorkspaceFiles` 异步存在性校验（realpath 落点校验防符号链接逃逸）；③ `pi-runtime.ts` `tool_execution_end` 对产出型工具异步 stat 校验后合并回填 `changedFiles`（与 changedFile 去重），仅 active 会话 emitState 推送；④ 渲染端 `changed-files.ts` 优先读数组（fallback 单文件）、按扩展名判 kind（image/file）；`ChangedFilesPanel` 改名「交付产物」（PackageOpen 图标），每行整行可点预览——图片行 Image 图标强调色、无 diff 按钮，代码行保留变更按钮；`WorkspaceTree` 即时刷新条件同步放宽到产物数组（新产物文件即时出现在左侧树）；demo-api 补生图产物演示（bash + rolldek → demo.png）。npm test 490 + build 全绿。**同日修复（2026-08-24 二修）：执行过程中产物面板误报「所有涉及文件（含读取的）」**——真实会话审计（tool-audit + 会话 JSONL）：bash 是 AI 最常用的读文件方式（1043 次 vs read 496），`cat`/`git diff`/`git log`/`ls` 的输出里充满工作区内真实存在的路径，旧逻辑对“非纯读类工具”的输出全量扫描 + stat 存在性命中 → git diff 里提到的 `package.json`、cat 出的 `docs/迭代记录.md`、`.json` 结尾的审计文件全被收成产物，执行到哪显示到哪；回合结束消息收敛后才恢复正常。三重收紧：① 新增 `artifactCandidatesFromBashCommand`——bash 优先从命令参数解析显式输出路径（`-o/--output/--save*` 的值、`> / >>` 重定向目标、`cp/mv/convert/tee` 目标），覆盖 rolldek/sensenova 等生图 skill 的 `-o xxx.png` 模式，这是“脚本把文件写到哪”的最强信号；② 输出扫描只认“保存类指示词”语境（保存/生成/写出/导出/下载/写入/Saved/Generated/→ 等）且窗口不跨行（前后 60/24 字符）——cat/ls/git diff/git log 的普通输出无此类语境不再误报；③ 排除集扩充——ask_question/recognize_images/subagent/memory_read 精确名 + `todo_`/`memory_`/`browser_` 前缀（清单/记忆/网页/问答文本路径误报率高）。真机回归：git diff/cat/ls 输出零候选，`-o outputs/fox.png`/`> build.log` 命中。npm test 492 + build 全绿）
* [X]  修复：新建会话执行后左侧迟迟不显示「执行中」话题（2026-08-24：根因——`session.new`→`createSession` 在稳态（已有本 Agent 会话列表）时有意跳过 `refreshSessions` 全量磁盘扫描（它在会话多/大会话时逐行读全部 JSONL 很慢），于是 sessionId 不在内存 `currentSessions` 里：新建会话左侧不出现；发消息后 `agent_start` 设 `runStatus="running"` 但 `patchSessionRunStatus` 是 `currentSessions.map(...)`，对不存在的 id 打不上点，只能等 lifecycle 事件 500ms 去抖触发全量扫描后才能带上——即“卡很久才出现执行中”的直接来源。修复两层、不再让新会话依赖磁盘扫描：① `session-scope.ts` 新增纯函数 `mergeSessionSummary`（按绝对路径去重、modifiedAt 降序，语义与 refreshSessions 的 Map 去重一致），并补 3 个测试用例；② `pi-runtime.ts` 新增 `ensureSessionInList`，在 `createSession` 末尾（await sessionsPromise 之后、emitState 之前）把新建会话以最简摘要立即合并入 `currentSessions`（仅当前列表作用域匹配本 Agent 时才插，切角色后仍走重拉建表；已在列表的会话不动）；新会话创建即见、「执行中」圆点在 prompt 的 patchSessionRunStatus 上即时打上，全量扫描退化为后台校正 title/排序。纯主进程逻辑变更，无协议改动，npm test 484 + build 全绿）
* [X]  缓解待办清单攒批更新：延迟不在管道在模型，三项手段对齐/超越 dsh 原版（2026-08-24：根因分析——推送管道逐环核实即时（todo-store.replaceAll→refreshTodos→emitTodos→渲染端 set 无任何节流），延迟 100% 来自模型侧：审计 18 会话实测两次 todo_write 平均隔 2.7~18.7 个工具调用；对照 dsh 原版确认其同样只有描述级软约束（"do not batch completions"）无防攒批机制——整表替换的成本结构 + 清单不进模型视野（保前缀缓存）是两边共有的结构性因素，官方模型靠训练对齐兑住、异构第三方模型兑不住。落地三顶：① 回合边界清空（dsh todo-plan-clears-on-next-turn 语义）：session.prompt 显式发起新消息时若清单非空则 replaceAll([]) 写盘+广播（磁盘/镜像/UI 三处一致，重开不复活），followUp/steering 队列注入与 regenerate 不算边界不清；② 攒批软提醒：runtime-todo-tools 新增 createTodoPaceTracker 每会话计数器，tool-audit 扩展 tool_execution_start 加 onToolStart 旁路回调喂计数（todo_write 自身 start 先 +1 后被 execute 内 consume 吞掉无需特判），返回文本在距上次更新 ≥PACE_REMINDER_THRESHOLD(4) 个调用时追加「提示：距上次清单更新已执行 N 个工具调用——一项完成立即标记 completed，不要攒批」（纯尾部追加不破坏前缀缓存）；③ 防截断渲染（dsh todo-parallel-in-progress 笔记的 summarySuffix 纪律）：action-timeline-call-summary 行工具名 strong 承担 min-width:0+ellipsis 收缩（兼容 mcp__server__tool 镓名），「状态 · 耗时」span 改 flex:0 0 auto+nowrap 非收缩槽位。AGENTS.md Todo 条目同步两新语义。新增 pace 用例 1 例（阈值边界/归零）。npm test 481 + build 全绿）
* [X]  左侧会话支持手动删除（2026-08-24：用户反馈会话列表只能新建/重命名/置顶、不能删除。落地：协议新增 `session.delete {path}` 命令；主进程按 `workspace.remove` 同款链路实现——校验路径落在当前 Agent 会话目录且为 .jsonl、销毁匹配的 live record（运行中会话一并销毁，语义与移除整个工作区一致，同时避免删文件后内存残留）、unlink 会话 JSONL 与 `todos/<sessionId>.json`（会话 id 取自会话列表、兜底按文件名推断）、删除当前正在使用的会话后自动补一个空白会话保持「当前话题」可用；渲染端在会话右键菜单（已有重命名/置顶）末尾加「删除会话」danger 项，弹「移除工作区」同款确认对话框（Trash2 + 不可恢复提示）防误删，确认后发 `session.delete`；demo-api 同步删除 mock。npm test 480 + build 全绿）
* [X]  输入栏 /指令候选框分组显示 + Skill 候选去前缀（2026-08-24：用户反馈斜杠菜单平铺且每条 Skill 都带 `/skill:` 前缀不好找——① 菜单按「会话指令 / 技能」两组渲染，复用模型下拉的 composer-menu-group 骨架，组标题吸顶、组间分隔线；② Skill 候选项只展示裸名（trigger 仍是 `/skill:<名字>` 供回填），匹配逻辑同时接受 `/sk` 前缀与裸名前缀两种输入（`/des` 能命中 design-taste-frontend）；③ 图标判断改按 cmd.kind/command.type 而非 trigger 字符串前缀，键盘 ↑↓/Enter 导航不变（分组渲染带扁平索引）。纯渲染端变更，npm test 480 + build 全绿）
* [X]  修复并完善内置渠道「拉取最新模型」：静态表渠道能拉到最新目录 + 无接口渠道不再假装成功（2026-08-24：用户反馈 opencode-go / z.ai coding cn 点「拉取最新模型」永远拉不到新模型。根因——SDK 的模型目录刷新（`ModelRuntime.refresh`）只覆盖实现了 `refreshModels` 的渠道（当前仅 Radius 网关），opencode-go/zai-coding-cn 等渠道的模型表是随应用版本打包的静态表，主路径 refresh 对它们无操作且 `result.errors` 恒为空 → 运行进程按「errors 为空即成功」误判完成，直连兜底从不触发，模型列表不动。修复：① `BUILTIN_MODELS_ENDPOINTS` 直连表补 5 个官方 OpenAI 兼容 /models 端点——opencode-go（`opencode.ai/zen/go/v1/models`，免认证）、opencode（`zen/v1/models`，免认证）、zai-coding-cn（`open.bigmodel.cn/api/coding/paas/v4/models`，Bearer）、minimax（`api.minimax.io/v1/models`）、minimax-cn（`api.minimaxi.com/v1/models`）；② 重构 `provider.models.refresh`：目标渠道在直连表则一律直连官方接口拉最新并写 SDK 目录覆盖层（静态表渠道唯一可靠路径）；Radius 保留 SDK 远程目录刷新；其余无官方列表接口的渠道（kimi-coding/ant-ling 等）给出明确错误提示「模型目录随应用版本更新」，不再误报成功；③ overlay 微调：已知模型保留内置显示名（opencode/minimax 的 /models 列表项只有 id 无 name，避免把内置 `MiniMax-M3` 退化成 `minimax-m3`），新模型仍克隆模板元数据。opencode-go/zen 端点实测 HTTP 200。npm test 480 + build 全绿）
* [X]  修复：内置服务商「仅勾选模型、留空 API Key」保存报 No API key（2026-08-24：配置页二次保存内置服务商（如 opencode-go）时 API Key 输入框留空沿用已保存 key——前端内置分支无条件发 `auth.set provider=""`，运行进程 `setRuntimeApiKey(provider,"")` 用空串覆盖已注入的运行态凭据，随后把会话切到勾选模型时 `checkAuth` 立即失败抛 `No API key for opencode-go/minimax-m3`；自定义服务商 `provider.save` 早有 `apiKey?.trim()` 保护不受影响；key 本身已持久化于 userData/credentials.json（判根因时核查实机文件确认 opencode-go/deepseek/openrouter key 均在），重启后重新注入故“又好了”，此即用户“key 没持久化”的错觉来源。修复两层防御、语义统一「留空 = 保留原 key 不覆盖」：前端仅在 `apiKey.trim()` 非空时才发送 `auth.set`；运行进程 `auth.set` 收到空 key 时跳过 `setRuntimeApiKey`（保留已注入 key，后续切模型逻辑照常，填新 key 时的切换行为不变）。npm test 480 + build 全绿）
* [X]  主题钩子契约同步：Skill/检查器/文档对齐代码现状（2026-08-22：审计发现 pidesktop-theme-creator skill 落后于代码——① token 少了 `--blockquote-surface`（styles.css 已消费，blockquote 底色脱离主题控制）：SKILL.md 83→84、variables.md 契约表、check_theme.py EXPECTED_VARIABLES、模板 CSS 明暗两块均补齐；② 检查器会误杀合法钩子：KNOWN_PANES 补 `memory-panel`/`workspace`，KNOWN_CONTROLS 补 `memory-toggle`/`queue-edit`/`queue-send-now`/`queue-remove`/`thinking-expand`/`context-usage`/`browser-pick`；③ 用户拍板将四个「代码有、契约无」的新钩子全部纳入公共契约：AGENTS.md 主题 bullet 与 theme-guide.md 钩子表/控件表同步收录（workspace 主工作区容器 + 思考块展开/上下文占用指示/预览元素选择三控件）。纯文档+skill 变更，无源码改动；npm test 475 + build 全绿）
* [X]  工具调用节点默认收拢：长会话不再被一排展开的调用详情淹没（2026-08-22：此前运行中的调用自动展开且结束后保持展开——接入 diff/高亮内容后每个节点都很高，滚动查看历史时满屏展开块。改为默认折叠：运行中的调用自动展开（实时进度反馈），结束瞬间自动收拢恢复折叠；用户手动展开/收拢的调用不受自动状态机影响。纯渲染端变更，npm test 475 + build 全绿）
* [X]  工具调用结果美化：编辑/写入不再淹没在 `\n` 转义 JSON 里（2026-08-22：用户反馈「编辑文件」调用指令整段 raw JSON（edits[].oldText/newText 带转义）看不出改了什么。落地：新增 src/renderer/src/lib/tool-call-preview.ts 纯函数（parseEditCallArgs 兼容 `{path, edits:[{oldText,newText}]}` 与旧 `{path,oldText,newText}` / parseWriteCallArgs / 行级 Myers diff O(ND) 上限 1500 行防爆 / edits 摘要），气泡工具调用节点新增「变更」区——优先渲染工具返回的统一 patch（execution.patch，带 ---/+++ 文件头与 @@ 行号上下文，复用 DiffView + 主题 --diff-* 颜色），运行中/无 patch 时按 edits 计算行级 diff（红删绿增 diff-add/diff-remove 类）；「写入文件」新增「写入内容」区展示解码后内容（20k 字符截断）；「调用指令」对 edit/write 改为紧凑摘要（path + N 处编辑 / N 字符），其余工具保持 raw JSON。新增 tool-call-preview.test.ts 18 例（Myers 替换/插入/删除/空串/CRLF/超限回退等）。npm test 469 + build 全绿。同日再扩：写入/读取内容语法高亮——read/write 工具的参数化内容（写入内容区与读取输出的文件内容）改用 RichContent 的 CodeBlock（highlight.js 按语言高亮 + 工具栏语言标签/复制按钮），语言由新增 languageFromPath 按文件扩展名推断（.ts→typescript、.yml→yaml、Dockerfile/Makefile/CMakeLists.txt 特殊名等 40+ 映射，未知扩展名走 highlightAuto；dotfile 不误判），60k 字符内不走截断、超出退化为纯文本块（截断提示）；parseReadCallArgs 配套。tool-call-preview.test.ts 增至 24 例。npm test 475 + build 全绿）
* [X]  修复：回答耗时/总耗时出现在回复气泡内（2026-08-22：用户反馈流式/完成态下 TimingMeta 渲染在 `.message-bubble` 内、与气泡内容混在一起，而底部行内进度（response-progress-inline）已有一份——两处同源重复。修复：耗时/压缩耗时统一定位为气泡外的 caption 行——MessageView 与 PendingResponse 中 TimingMeta/CompactTimingMeta 从气泡体内移出，作为 `.message` 网格第二列新行渲染（`.message-assistant > .message-timing { grid-column: 2 }`，与气泡左缘/底部行内进度对齐）；`showTurnTimingOnLatest` 改为 `turnTiming && !isGenerating`——回合进行中只由底部行内进度展示实时耗时，结束后在最新一条回复气泡下方展示定格值，气泡内容不再出现耗时文本。纯渲染端变更，npm test 451 + build 全绿）
* [X]  修复：流式输出（尤其思考过程）期间气泡持续闪烁（2026-08-21：动效层上线后暴露——根因不在 CSS，而是 Pi agent-loop 每个流式事件都携带 `{ ...partialMessage }` 新对象、提交时 `response.result()` 又是另一对象，旧 messageUuid 按「对象身份 WeakMap + 自增序号」发 uuid，导致同一条消息每个流式帧 uuid 都不同；渲染端 `key={message.uuid}` 逐帧失效 → 整个 MessageView 每 chunk 重挂载，新加的入场动画每次重播即持续闪烁；store.ts 按 uuid 合并的优化也因此从未生效。修复：uuid 改为 `${timestamp}-${role}-${index}` 确定性生成——provider 的 output 对象在流开始时创建一次 timestamp 固定、index 在追加式数组中对消息恒定，故全部帧与提交后天然一致；纯函数抽到 src/main/message-identity.ts 附 4 例单测。npm test 451 + build 全绿）
* [X]  UI 美化第三弹：动效层 + 全局过渡治理（2026-08-21，design/ui-motion 分支：① 全局 * transition 收窄——原先所有元素的 bg/color/border/shadow 变化都带 .22s 过渡，流式渲染每个新气泡与长列表重绘均付过渡成本（与已移除的 per-bubble blur 同类隐患）且 hover 反馈发糊；改为 button/input/select/textarea/summary/label 统一 .18s（含 transform 支持按压触感），另补 composer/browser-address/model-search-box/sidebar-search 四个 :focus-within 焦点容器；② 新增动效全部 transform/opacity + cubic-bezier(0.16,1,0.3,1)：消息入场淡入上移 6px（React 复用流式 DOM 节点只在挂载播一次）、panel-dock 滑入、question-panel 曲线升级、五类弹出菜单统一 menu-pop-in、主按钮/图标按钮 ：active 按压 scale 触感；③ prefers-reduced-motion 下装饰动画全关停，保留 .spinning 加载指示语义。纯 CSS 变更，npm test 447 + build 全绿）
* [X]  修复：启动即弹「自定义服务商需要填写名称和接口地址」且会话列表加载不出来（2026-08-21：根因是 428fb9d 引入的内置服务商模型可见性条目（`custom: false`，按协议注释这类条目只记录每模型勾选、`baseUrl` 恒为空）落进 settings.providers 后，utility 进程 `initialize()` 仍对每条 provider 无条件调 `registerCustomProvider`——空 baseUrl 触发校验抛错，整个 initialize 在 `createSession()`/`refreshSessions()` 之前中断，命令队列 catch 把错误 post 成弹窗，于是「弹窗 + 无会话」两个症状同源；只要用户在设置里保存过一次内置服务商的模型勾选（新增的全选/搜索让这操作更顺手）下次启动必现。修复：校验+注册参数构建提纯为 custom-provider.ts 的 `resolveCustomProviderRegistration`（`custom: false` 返回 null 跳过注册，内置服务商务必不能被 registerProvider 用假 OpenAI 兼容定义遮蔽目录原本条目），pi-runtime 的 `registerCustomProvider` 变薄壳；initialize 循环不变（条目 id 照常进 initializedProviderIds、API Key 照常回灌）。新增回归测试 4 例（custom:false 跳过 / 空 name|baseUrl 抛错 / 非法 URL / trim+启用过滤）。npm test 447 + build 全绿）
* [X]  UI 美化第一弹：字号与可读性治理（2026-08-21，design/ui-audit：全量审计后用户选定先落地字号项——styles.css 中 42 处 9px 清零，按新规范分流：CJK 连续阅读文本（会话 meta、菜单描述、表单标签、MCP/钩子说明、主题卡片描述等 13 处）升 11px；数字徽章/kbd/纯拉丁元信息（计数胶囊、保存状态、路径 meta 等 25 处）升 10px 保持胶囊紧凑；唯一例外是主题预览缩略图（.theme-preview-*）属微缩模型按比例缩小，加注释防止误"修复"。字号层级规范写入 styles.css 头部注释与 theme-guide.md 字体章节：正文 14 / 输入框与次级 13 / 列表主文本 12 / 辅助 meta 11 / 徽章类最小 10。同日审计报告另产出待落地方向：圆角四档收敛、动效层、聊天空状态 chips、强调色校准（暂不动）。纯 CSS+文档变更，npm test 443 + build 全绿）
* [X]  AI 浏览器自动化 + 手动元素选择 + 设置页模型列表搜索（2026-08-21，feature/browser-automation：① browser_* 工具集（navigate/snapshot/click/type/press/scroll/eval/screenshot/wait/get/tabs）让 AI 操作用户可见的内置浏览器——工具在 utility 进程只做参数校验与结果翻译，每个操作经 browser-automation.request/result RPC 对到主进程 BrowserAutomationController，用 Electron 内置 webContents.debugger 走 CDP（Runtime.evaluate / Input.dispatchMouseEvent / Input.insertText / Page.captureScreenshot），不引 Playwright 依赖；会话首次使用时绑定标签页（前台页或新建 pi-browser-<uuid> 页并推 browser-preview:tabs 让预览面板显示激活），每页忙锁串行并发操作；snapshot 返回 @e1..@eN 引用但引用不是稳定句柄——注册表只记元素签名，click/type 前重新定位并校验签名，失配报「重新 snapshot」可行动错误（agent-browser 引用生命周期语义）；元素收集递归穿透开放 shadow root 与同源 iframe（稳定深度优先遍历保证 ref 序号可复现），跨 frame 坐标换算到顶层视口、遮挡检查在元素自身根空间执行；快照封顶（200 元素/3000 字页面文本）并包不可信页面内容标记；截图返回图片部件（多模态模型直看，纯文本模型走 stripContextImages 占位指向 recognize_images）；权限：browser_navigate 与写入型 browser_eval 带 browse 风险过权限门（read-only 模式拒绝），页面内操作信任直接执行，browser_tabs 关闭动作同样过门；总开关 settings.browser.enabled 缺省启用、逐调用实时读（工具常驻注册不重建会话），通用设置页加勾选项；操作期间预览页顶 automating 横幅（BrowserPreviewState.automating）；子代理无浏览器工具。② 手动元素选择：预览工具栏准星按钮开 pick 模式（BrowserPreviewCommand pick-mode 转发到 WebContentsView 的独立 preload——electron-vite 第二个 preload 入口 out/preload/browser-pick.cjs），悬停描边、下一次可信点击在点击位置弹出封闭 shadow DOM 迷你确认卡（页面 CSS/JS 够不到，Enter 提交 IME 排除/Esc 取消，同源 iframe 内在 iframe 里弹卡带 iframe URL）；提交送 {url, element, note}（element 含 tag/CSS 选择器 path/role/type/name/text/href/src，主进程 parseElementPickMessage 形状校验，备注 2000 字封顶）；CDP 注入的点击 isTrusted 也为真无法区分，主进程在 automating 横幅期间丢弃 pick 结果防误采；选中元素经 browser-preview:pick 推渲染端，composePickMessage 拼【来自内置浏览器】块（页面/元素/路径/链接/图片/内容+备注）直接追加进聊天输入框并聚焦光标置尾，空备注只发元素块，多次点选顺序追加。③ 设置对话框「可用模型」列表工具栏：全选复选框（indeterminate 三态，作用于当前搜索可见项——OpenRouter 数百条长列表先全取消再搜出想要的几个勾上）+ 已启用计数 + 模型数 >8 时出现搜索框（名称/ID 大小写不敏感过滤）；逻辑提取到 lib/model-list.ts 纯函数（filterProviderModels/setProviderModelsEnabled/buildBuiltinProviderEntry）便于单测。新增 browser-automation.test.ts 12 例 + runtime-browser.test.ts 12 例 + browser-preview-pick.test.ts 7 例 + browser-pick（preload/renderer）测试 16 例 + model-list.test.ts 9 例 + permissions browse 风险用例。npm test 443 + build 全绿）
* [X]  钩子设置页按钮布局优化（2026-08-20：头部右侧「启用钩子」开关与「新建钩子」按钮两个控件顶在右上角、与左侧较长的说明文字块垂直错位、右侧留白失衡——改为头部只留启用开关（随标题行对齐），「新建钩子」移入「规则」区段标题行（显示 N 条 + compact 按钮，与 MCP Server 页「添加」按钮同一模式，空状态提示文案仍指向该按钮）；纯 JSX 结构调整，无协议/CSS 变更。npm test 379 + build 全绿）
* [X]  钩子系统：会话生命周期事件的用户可配置自动化（2026-08-20：第三个 app-owned 内联扩展 pidesktop-hooks 挂在权限门与工具审计之间的 extensionFactories——事件 session_start（命令被顺序等待，环境准备语义）/ tool_call（阻断型：block deny 正则在进程内评估；阻断型命令退出码 2 或 stdout `{"block":true,"reason"}` 即否决，与权限门同款 `{block,reason}` 契约、两道门叠加生效）/ tool_execution_end（改完即格式化，args 由 start 事件暂存）/ agent_end（回复结束——整次 agent run 只触发一次，携带累计 usage/总耗时/失败标记，跑完通知挂这里）/ turn_end（单轮调用结束——每个模型调用小轮触发，一次回复多次，大多数场景应选 agent_end；起因：用户反馈首版把通知挂在 turn_end 上导致一次长回复弹多次通知，遂补 agent_end 事件并把示例钩子/面板默认事件迁过去）；动作四类 notify（桌面通知）/ http（POST 事件 JSON，10s 超时）/ block（deny 正则）/ command（shell 逃生舱）。配置双作用域 JSONC：项目 `<workspace>/.pidesktop-hooks.json` 覆盖全局 `<agentDir>/pidesktop-hooks.json`（按 name 合并，mcp-config 同款 JSONC+原子写）；事件 handler 触发时读模块级缓存，因此 hooks.save/toggle/delete 只刷新缓存+重发资源目录——不重建会话、也不拒绝运行中编辑（用户可能正想中途停用危险钩子）。命令是用户自写配置，走 terminal 同款信任不经 agent 权限门：utility 进程 spawn shell 执行、stdin 收完整上下文 JSON + HOOK_EVENT/HOOK_SESSION_ID/HOOK_WORKSPACE/HOOK_TOOL 环境变量、超时 1–120s 默认 10s（Windows taskkill /T 杀进程树）；钩子任何失败记 warn 日志按放行处理不伤回合；缓存纪律——不暴露 before_agent_start/context 注入面，钩子输出绝不进系统提示词。通知经 hook-notify 推送（携带触发会话 sessionId）由主进程 Electron Notification 显示（点击聚焦主窗口，项目首个桌面通知用例；聊天软件同款免打扰——通知关于正在查看的会话且窗口聚焦未最小化时自动不弹，只在窗口失焦/最小化或后台会话跑完时提醒，面板"测试"用虚构 sessionId 恒显示），utility 进程无 Electron API 故必须主进程执行。设置对话框新增第六个「钩子」标签页（HooksSettings.tsx：总开关/规则列表（事件·动作·作用域·拦截标记）/编辑表单/共享测试样例行 + 按规则测试 hooks.run→hook-run 推送回显结果）；settings.hooks.enabled 缺省视为启用、实时生效；ResourceCatalog 增 hooks/hooksEnabled 字段随现有 resources 推送；demo-api 同步模拟。新增 hooks-config.test.ts 7 例 + runtime-hooks.test.ts 14 例（含真实 shell 命令的退出码/超时/JSON 否决语义）。npm test 377 + build 全绿）
* [X]  顶部栏显示当前 git 分支（2026-08-21：topbar 项目名右侧新增分支徽章（GitBranch 图标 + 分支名，accent-soft 胶囊样式，带悬浮提示）；协议 `RuntimeSnapshot` 新增可选字段 `gitBranch`（非 git 项目不提供，渲染端缺省不渲染，向后兼容）；主进程新增 `src/main/git-branch.ts` 只读不依赖 git CLI——普通仓库读 `.git/HEAD`，linked worktree 场景 `.git` 是文件时按 `gitdir:` 条目解析相对/绝对路径，detached HEAD 显示提交短哈希，任何读取失败/非 git 返回 undefined；pi-runtime 模块变量 + `refreshGitBranch()` 异步刷新（读盘期间切换工作区即丢弃过期结果，值未变不发快照），挂载在工作区激活汇聚点 `activate()` 与 `disposeRecord` 清空激活槽处——打开/切换工作区或话题时更新，不做常驻监听（用户未选实时刷新选项，采用推荐方案）；demo-api 同步模拟。新增 git-branch.test.ts 9 例。npm test 356 + build 全绿）
* [X]  壁纸模式下消息气泡半透明化，壁纸不再被全底色挡住（2026-08-21：配置壁纸（html[data-theme-wallpaper="true"]）时聊天气泡默认全不透明底色挡住了壁纸——新增壁纸模式规则把 AI 回复气泡、用户气泡及内嵌块（代码块/折叠项/reply-files/mermaid/html-bubble/扩展消息 callout 等）改为 color-mix 纯 alpha 半透明混色，不引入 backdrop-filter（每气泡 blur 曾因流式重绘/长列表滚动性能被移除，纯 alpha 无此成本）；主题预览气泡同步生效；无壁纸主题完全不受影响，主题作者可用 [data-theme-wallpaper="true"] 选择器覆盖。纯 CSS 改动 + theme-guide.md 壁纸章节说明。npm test 356 + build 全绿。同日：气泡透明度配置化——外观设置新增「气泡透明度」滑块（协议 AppearanceSettings.bubbleOpacity: {light?,dark?}，沿用壁纸透明度明暗双 tab 模式），统一控制壁纸模式下气泡与内嵌块底色透明程度，默认 80%、可调 40%~100%（低于 60% 提示影响文字可读）；theme-presets 新增 bubbleOpacityCss 注入 --bubble-alpha 变量（主文档与主题预览双处注入），styles.css 硬编码分级数值收敛为单一默认 var(--bubble-alpha, 80%)；重置按钮恢复应用默认。同日再扩：面板透明度配置化——外观设置新增「面板透明度」滑块（协议 AppearanceSettings.panelOpacity: {light?,dark?}），用独立变量 --panel-alpha 控制壁纸模式下左右上下栏（sidebar/topbar/right-panel/composer）及对话框（settings/permission/content-preview）的 --panel-bg 保留比例（color-mix 混色，默认 100% 即主题原样、可调 40%~100%），不覆盖主题 --panel-bg token、不碰 backdrop blur；theme-presets 新增 panelOpacityCss 双处注入。新增 theme-presets 测试 2 例，npm test 381 + build 全绿。修：气泡/面板透明度滑块静默无效——App.tsx 主题注入 effect 依赖数组漏了 bubbleOpacity/panelOpacity，滑块变更不触发 style 重生成，--bubble-alpha/--panel-alpha 从未注入（壁纸透明度因一直有 wallpaperOpacity 在依赖里未暴露）；已在把两字段加回依赖数组，实测生效。再修：设置页左侧导航列/右面板顶部标签条/侧栏分页条自带不透明背景未纳入壁纸规则——把 .settings-tabs/.panel-tabs/.sidebar-tabs 补进面板透明度组；并用宿主同内核实测确认 color-mix(..., var(--panel-alpha), ...) 在 Electron 43.2.0 级联正常（四个面板 computed 均为半透明白、69% 时 92%×69%≈63.6%），主界面侧栏滑块逻辑本身没问题——69% 相对默认 92% 白度差本就小，侧栏内容密肉眼不易分辨，拖到更低才直观可见。再扩：右侧预览主体纳入面板透明度——content-preview-body / preview-markdown / preview-code / preview-plain-text / preview-image / preview-pdf 壳底色改 color-mix(..., var(--panel-alpha))，壁纸透进预览区；iframe 内网页/渲染后 PDF 属内容本身保持不透明；markdown 编辑器（vditor）与终端暂不纳入（工具读密集，可读性优先，需要再说）。再补：右侧预览「空状态」与标签栏纳入面板透明度——preview-empty-state（新建预览引导页，--surface-muted）铺满时是纯色，单独补纳；preview-tabs 标签栏（--surface）同步，空状态卡片按钮保留不透明（可点控件））
* [X]  ask_question 提问面板分页式改造 + 首选项推荐标记（2026-08-20：QuestionPanel 按参考图重构——一次只展示一题，左上「下一步/最后一步」步骤标签、右上 X/N 页码 + ◀▶ 箭头跨页切换（草稿跨页保留）、左下快捷键提示「使用 Tab / 上下键选择，回车或空格选中」、右下「忽略」（取消整个提问）/「继续」（翻页，末题提交所有答案）；选项从胶囊横排改竖排编号行（单选↓↑移动焦点同步改选），自定义输入框编号续接选项、占位「输入你的回答...」；选择题第一个选项自动标注「（推荐）」，与 ask_question 工具描述新约定一致——模型把最推荐选项放第一位，无协议变更（推荐不占用 answers 语义）；runtime-question-tool 描述与 schema 选项说明同步更新，并新增测试断言该约定；theme-guide/AGENTS.md 文档同步。npm test 347 + build 全绿。同日微调：选项行与自定义输入行改「幽灵框」——默认透明无框，鼠标悬停或 Tab 聚焦（:focus-visible/:focus-within）才显背景边框，自定义输入编号包进框内不再悬在框外，选中项仍保持 accent 高亮。再调：单选点选/回车/空格即自动进入下一题（末题自动提交，页码可翻回改答），多选保持勾选多个由「继续」或输入框回车推进（避免点一即跳页无法多选）。修：单选点选中已选项会被当成取消而不跳页——改为单选任意点选都选中并立即推进（取消逻辑移除，重选直接换个选项；↑↓ 移动选中不跳、回车确认即跳））
* [X]  待办与记忆面板合并为会话区右上角「面板坞」（2026-08-20，feat/vision-tool-call 内提交 d28816c：两面板各有 FAB、打开后同在右上角区域（right 14px/58px），都有内容时互相遮盖且无法同时展开。新增 PanelDock——关闭态单个 FAB（NotebookTabs 图标，badge 同显待办未完成数+记忆主题数，运行中终端沿用小图标）；打开态浮动面板坞头部 tab 栏（待办默认在前）切换两个内容页，两页始终挂载 hidden 切换、各自编辑/展开状态不丢。TaskPanel/MemoryPanel 改为无外壳内容组件 TaskPanelContent/MemoryPanelContent，.task-panel/.memory-panel 由绝对定位改面板坞内填充布局（宽度统一 340px、max-height min(70vh,560px)）；主题钩子 data-pane task-panel/memory-panel 与 data-control task-panel-toggle/memory-toggle 语义保留迁移，theme-guide.md 同步。npm test 346 + build 全绿）
* [X]  修复：recognize_images 在模型切换后不激活（2026-08-20，feat/vision-tool-call 内修复提交：首版 a3cd649 遗漏了 reconcileVisionTool 的 6 处 setModel 调用点——多模态会话切纯文本模型后工具不在激活列表，附图只能入槽无人可调，功能整体失效。新增 switchSessionModel 收口 setModel + reconcile，6 处调用（顶栏模型选择 / 保存 API Key / 服务商增删改 / 设置保存）全走它；轮末兜底提为 discardUnconsumedVisionInbox 覆盖 agent_end 与 agent_settled（中断收尾时未识别图片作废，防陈图泄漏进后续轮次）；readImageFile 改为绑定会话自身工作区，后台驻留会话切工作区后不再读错目录）
* [X]  视觉识别工具化：非多模态模型的图片输入从预处理兜底改为 recognize_images 工具调用（2026-08-20，feat/vision-tool-call：用户附图暂存会话级待识别槽 + 用户消息尾部提示模型调用工具；工具支持 files 参数直读工作区图片（截图自省闭环），realpath + workspaceRelativeAttachment 越界拒绝 + 20MB + 格式白名单；工具定义字节恒定、仅在会话创建/模型切换时按会话模型是否支持图片重算激活集合（reconcileVisionTool，5 处 setModel 后统一调用），动态图片数只进对话尾部不碰前缀缓存；删除专用 pending 气泡与 runVisionFallback（走通用工具执行 UI）；轮末未消费图片记 warn 日志并作废；排队带图统一拒绝；AGENTS.md 架构描述同步更新；新增 runtime-vision.test.ts 11 例 + vision.test.ts 标签用例，vitest 333 全绿、build 过）
* [X]  markdown编辑器，右侧预览的文件可以实时编辑，并且实时同步ai的变更
* [X]  本次变更的附件那里应该给两个按钮，一个预览按钮，一个显示变更变化的按钮
* [X]  新建工作区后没有立刻在侧边栏生成对应的工作目录
* [X]  切换会话时速度特别慢，存在比较严重的性能问题
* [X]  todo悬浮面板修改支持更多内容，支持显示后台运行的终端
* [X]  右侧预览标签页切换标签时，如果其中一个标签时浏览器，切回来的时候页面消失了，需要重新刷新页面才行
* [X]  输入框支持@选择工作区文件（2026-08-18：主进程 searchWorkspaceFiles 递归索引+评分搜索+写操作失效缓存，渲染端 @ 提及菜单 ↑↓/Enter/Tab/Escape 导航、IME 防误触、目录以 / 结尾钻取；选中的文件以 chip 气泡形式显示在输入框内（同 Skill chip，可多个、可移除），发送时拼回 @相对路径）
* [X]  上下文占用情况数据显示（2026-08-18：协议新增 `ContextUsage{tokens,contextWindow,percent}` 挂在 `RuntimeSnapshot.contextUsage`，数据源直接复用 `AgentSession.getContextUsage()`（最后一次 LLM 响应真实 usage + 其后消息 chars/4 估算，压缩边界处理与 pi CLI 一致）；输入框左侧新增「上下文 62.5%」chip，>70% 警告色、>90% 危险色（复用主题变量深浅色自适应），悬浮显示 `约 80.1K / 128K tokens`；压缩后 tokens 未知时显示 — 并提示下一条回复后更新；demo-api 同步模拟该生命周期。同日追加缓存命中率：`ContextUsage.cacheHitRate`（cacheRead/(input+cacheRead+cacheWrite)，与 pi CLI CH 同源），主进程从活 transcript 倒序找最后一条有效 assistant usage 计算（跳过 aborted/error/全零，独立于压缩后 tokens=null），chip 内加「缓存 93%」段，无 usage 时不显示；命中率口径为会话累计 ΣcacheRead/Σ(input+cacheRead+cacheWrite)（对齐 claude-stat/deepseek-harness，瞬时值会随工具结果回填波动）。dsh 调研后升级为 durable projection 语义：计数器独立于 transcript 挂在 SessionRuntimeRecord 上（message_end 增量 + agent_end 重扫兑底），压缩不清零、regenerate 截断后重扫自愈，O(1) 替代每帧全量扫描）
* [X]  切换会话，新建会话时，右侧边栏不要自动收回。保留状态（2026-08-19：会话切换时不再 setPreviewOpened(false)，只清理会话级标签 artifact/diff；browser/terminal/file 全部跨会话保留——浏览器视图由 BrowserPreview 组件失活时隐藏、激活时恢复，销毁只发生在显式关标签/关面板时）
* [X]  当拉伸右侧边栏时聊天窗口的输入框下面的 访问权限等内容显示被挤压到一起了（2026-08-19：composer-footer 及左右两组加 flex-wrap，footer-right 改 flex:1 1 auto 保证换行后靠右；空间不足时控件组整体换行而非硬挤，composer 高度由既有 ResizeObserver 自动同步到 --composer-space/--composer-height）
* [X]  右侧预览面板支持 PDF 文件预览（2026-08-19：主进程注册自定义协议 pidesktop-file:// 流式读取工作区 PDF（protocol.handle + Web Stream，大文件不占渲染内存），渲染端 iframe 内 Chromium 内置查看器渲染；readWorkspaceFilePreview 对 .pdf 返回新 kind "pdf"（不读内容），协议层二次 realpath 校验防路径穿越；demo-api 同步 mock；新增协议 URL 往返/越界测试）
* [X]  AI 回复期间输入可回车排队发送，输入框上方显示排队输入列表（2026-08-19，交互/视觉对齐 ZCode 排队输入：模块标题「排队输入 N 条 · 本轮回复结束后自动发出」，生成中 placeholder「连续输入以排队后续修改」，每条右侧「立即」（Zap+文字强调按钮）/铅笔编辑/垃圾桶删除。实现直接复用 Pi SDK 原生 steering/followUp 队列——回车走 `session.queue.add` → `AgentSession.prompt(text,{streamingBehavior:"followUp"})`，本轮回复自然结束后消息作为下一轮 user 消息注入（延续同一次 agent run，后台停放会话照常投递）；「立即」=`queue.sendNow` 整队重建把目标提升为 steering（当前回合下一次模型调用前插入，不打断工具执行，条目标记「即将插入」置顶），编辑=回填输入框+`queue.remove`，删除=`queue.remove`（Pi 无单项删除 API，clearQueue 后按原序重建）；快照新增 `queuedMessages[{kind,index,text}]`（实时读 getSteeringMessages/getFollowUpMessages，queue_update 走 default 分支即时 flush），命令以 kind+index+text 寻址防列表漂移误伤；点停止时主进程 abort 清空队列（Pi CLI 同款语义），渲染端把队列文本回填/追加到输入框草稿供编辑重发；排队消息支持文本/@引用/文件附件/Skill（附件折叠进文本），暂不支持图片附件（Pi 队列纯文本存储，整队重建会丢图）；demo-api 同步模拟）

## 2026-08-16 工具管道优化（refactor/tool-pipeline）

借鉴 deepseek-harness 架构思路的三阶段落地（此前分析结论：dsh 的"跟随系统主题"建议不成立——本应用已有且为默认；插件注册表收益低，真正的痛点是 customTools 固定于会话创建）：

1. **工具审计 + MCP 调用超时**（`tool-audit.ts` / `mcp-client.ts`）
   - 新增 app-owned 内联扩展：`tool_execution_start/end` 事件 → 追加写 `chatanytime-sessions/<agentId>/tool-audit.jsonl`（时长/错误标记/参数摘要 2KB 截断，best-effort 串行写入）
   - MCP `callTool` 由此前完全无超时 → `AbortSignal.any` 合并外部取消与 120s 超时，超时报「MCP 工具调用超时」
2. **MCP 工具热重载**（`tool-delta.ts` + `applyMcpToolChanges`）
   - 关键机制（已用真实 AgentSession 集成测试钉死）：Pi 以引用保存 customTools 数组且刷新时重读；`pi.registerTool()` 可在活会话上触发注册表重建
   - 新增/替换服务器：原地换数组 + registerTool + setActiveToolsByName，消息流不中断
   - 删除/停用（无移除 API）：回退到原有会话重建路径（保 SessionManager，历史不丢）
3. **pi-runtime.ts 拆分**（1528 → 1416 行）
   - 五个能力簇搬至 `runtime-todo-tools / runtime-skills / runtime-vision / runtime-permissions / runtime-mcp`，纯构建器 + 显式入参，无共享可变状态对象
   - 死代码清理：`builtinToolNames`、恒空的 `resourceDiagnostics`

验证：vitest 232 全过（新增 14：审计 5、信号 2、delta 3、热重载真实会话集成 2、原 mcp 用例 2）；`npm run build` 过。无打包行为变化。

## 2026-08-16 多会话并行 + 会话状态点（feat/multi-session）

痛点：`createSession` 切换即 `dispose()` 旧会话——一个会话执行中无法新建/切换到另一个会话并行执行；侧边栏也看不出哪些会话在跑、跑完是成是败。

1. **会话运行时记录化**（`pi-runtime.ts`）
   - 原全局单例状态（session/busy/status/executions/controlMessages/turnTiming/pendingVision/todoStore/extensionApi/sessionCustomTools/权限门 deps）全部收进 `SessionRuntimeRecord`，`liveSessions: Map<sessionId, record>` + `activeRuntime` 激活指针
   - 切换/新建会话 = park 旧记录（后台继续流式写入自己的 JSONL），不 dispose；快照始终取激活记录
   - 每记录独立的 customTools 数组（MCP 热重载只作用于激活会话）、独立权限门（workspace/agent/session 按记录捕获）、独立 session-scoped todo store
2. **并行执行语义**
   - `session.new` / `session.open` / `workspace.open` / `agent.select` 忙时不再阻塞或杀运行中的会话；`session.open` 与 continueRecent 命中活记录时原位 reactivate（不重建，执行中的回合完整保留）
   - 同一会话内仍串行：prompt/regenerate/compact 增加 record.busy 守卫
   - 空闲驻留记录 LRU 上限 4 个（运行中永不驱逐）；`workspace.remove` 连带销毁该工作区的活记录；createSession 双重 generation 复查防止并发切换时旧管线抢回激活位
   - 后台记录只上报生命周期变化（省 IPC），流式事件直接丢弃
3. **会话状态点**（协议 + 渲染层）
   - `SessionSummary.runStatus?: "running" | "completed" | "failed"`，由 agent_start/agent_end（末条 assistant errorMessage 或 abortRequested 判失败）/prompt 拒绝/compaction 结果驱动，`refreshSessions` 叠加活记录状态
   - 侧边栏话题项加小圆点：黄=执行中（呼吸动画，实时状态）、绿=执行完成、红=执行失败（中止也算失败），复用主题 `--warning/--success/--danger`，深浅色自适应
   - 终态圆点（绿/红）语义是"未读结果提醒"：只在后台会话上挂，点进该会话即清除（结果已可见于对话）；正在查看的会话自己跑完不挂终态圆点（`setTerminalRunStatus` / `activate` 统一收口）
   - 会话完成/失败后 500ms 去抖刷新列表（标题/排序跟上）
4. **其他**：agent 切换/资源重载期间的 busy 反馈改走 `transitionStatus` 覆盖层（重建中记录被替换不再闪断）；助手切换按钮解除 busy 禁用；demo-api 模拟 running→completed 生命周期

验证：vitest 232 全过；`tsc` node/web 双工程通过；`npm run build` 过。

* [X]  修复：模型列表筛选保存后重进设置页只剩已启用模型、搜索框消失（2026-08-24：主进程 refreshCatalog 构建推送目录时直接 filter 掉 settings.providers 中 enabled === false 的模型——顶栏选择器因此正确只显示启用的模型，但设置对话框「可用模型」列表也从这份目录构建，筛选保存后重进时目录只剩上次启用的几个（providerModels.length ≤ 8 触发搜索框隐藏条件、其余模型不可见；勾选数据本身完好存于 settings.providers）。修复改为「标注而非过滤」：协议 ModelOption 新增可选 enabled 字段（缺省视为启用，向后兼容），主进程 model-catalog.ts 新增 buildCatalogModels 纯函数构建完整目录并逐条标注勾选状态，渲染端 model-list.ts 新增 selectableCatalogModels 供选择器类消费方统一过滤 disabled（顶栏/输入框模型菜单、全局默认模型下拉、Agent 默认模型下拉、视觉模型下拉、视觉兜底可用性检查）；设置对话框列表构建逻辑不变，因目录完整而自然恢复。新增 buildCatalogModels 测试 3 例 + selectableCatalogModels 测试 2 例。npm test 480 + build 全绿）
* [X]  主题钩子契约第三批扩展 + 面板底色分解 token：结构主题可及面继续下沉（2026-08-24：① 钩子扩充——侧栏行级 data-row-kind（workspace/session/agent）+ 布尔 data-row-active/data-row-expanded、时间线节点级 data-node-kind（thinking/tool-call/text）+ data-node-state（running/completed/error）、composer 分区 data-composer-zone（queue/attachments/error/input/footer/popup，popup 绝对定位浮层禁止主题改 position）、空态构图 data-pane="landing"、<html> 布尔状态补 data-ui-attachments、首个带值状态 data-ui-sidebar-view=topics/files/agents、装饰层元素公开 data-theme-layer/-kind 属性；② token 扩充——面板底色按面分解为 --panel-bg-sidebar/topbar/composer/dialog/preview 公开 token（默认 var(--panel-bg) 使用点解析，明暗板只覆写 --panel-bg 即联动），壁纸模式基础规则降为 [data-theme-wallpaper] (0,2,0) 特异性并逐面拆开（content-preview-panel 归 preview 面），新增 --layout-sidebar-width 布局量（desktop-shell 列宽改由它驱动）；③ AGENTS.md 主题 bullet 与 theme-guide.md 同步收录全部新钩子/token/覆盖特异性说明。顺带修复：设置页助手管理列表与侧栏 nav 共用 .agent-list 类名，瓷底背景规则互相命中（深色主题下立即显现）——设置页拆分为 .settings-agent-list。npm test 480 + build 全绿）
* [X]  深海女仆工坊 maid-atelier 主题移植：首个外部皮肤结构复刻样本（2026-08-24：从 DeepSeek Harness 皮肤 dsh-deep-whale/maid-atelier（作者 Small-tailqwq，CC BY-NC-SA 4.0）移植，themes/maid-atelier/ 纯 CSS + 21 个素材资产零脚本——双女仆立绘走 --pi-layer-* 声明层（暗色用预调暗 *-dark.webp 变体替代 CSS filter）、着陆/对话两档构图用 :root:not([data-ui-chat-empty]) 级联纯 CSS 切换、顶栏织带蝴蝶结/底部饰带徽章/侧栏画框四角/Q 版吉祥物全靠区域伪元素与装饰层、蕾丝按钮 border-image 九宫格直迁、明暗双模式全套语义 token；复刻过程反哺钩子契约扩展条目）。check_theme 校验通过）
* [X]  主题创作能力分析报告：docs/主题创作能力分析.md（2026-08-24：以 maid-atelier 复刻为样本盘点主题系统能力——结论为架构不需重构，需语义钩子下沉 + 确定性验证闭环 + 修真坑；成立能力清单（token 双模式换肤/声明式装饰层/data-ui 状态级联/border-image 九宫格/相对路径资产分发等）与按责任方归类的洞清单）

# 待办（未完成）

* [ ]  多模态不支持上传pdf等附件
* [ ]  增加计划模式，先生成计划再进行编码，计划模式会在当前工作目录下新建plan目录并新建计划md文件
