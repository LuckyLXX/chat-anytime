# PiDesktop 记忆系统实施方案（v2，对齐 dsh 缓存哲学）

> 状态：已实现（2026-08-19，分支 feat/memory-system；npm test 320 用例全绿、npm run build 通过；打包冒烟待手动验证）。
> 背景：分析过 GenericAgent（四层金字塔记忆 + 存在性索引）与 Proma（知识路由 + 提示词治理）两个开源项目的记忆模块后定稿。
> 落地偏差一处：面板入口采用会话区右上方浮球（与 task-panel 同族，`data-control="memory-toggle"`），未放顶栏 runtime-controls——与任务面板形态一致、App.tsx 改动最小。

## 核心原则（延续 todo 重写的方向）

**动态状态不进系统提示词；一切变化通过工具调用参数/结果出现在对话尾部（纯追加，不破坏前缀 KV cache）。** 系统提示词只含字节冻结的会话级常量。

记忆索引的特殊性：它是**跨会话的加载态**而非会话内的变化态（变化稀疏——只在记忆写入时变）。因此：

- **会话创建时一次性快照注入索引**：在 `createSession` 里取 `indexMarkdown(workspace)` 存入闭包变量，`systemPromptOverride` 拼入。整个会话字节不变 → **会话内缓存零扰动**；跨会话只有索引真变了才不同（新会话本来就是新前缀）。与 skillsBlock（`pi-runtime.ts` 的 systemPromptOverride 组装处）同类：会话级常量。
- **会话内新鲜度走工具输出**：`memory_write` 的结果回显该主题的最新索引行（不回整个索引，照 todo "结果只回计数" 的克制）；需要全量时 `memory_list`。工具结果在历史尾部，追加不破坏前缀。
- **没有 `before_agent_start` 每轮注入**，不建 context extension。
- 面板手改记忆（IPC 直改 store，不经过模型上下文）后，本会话快照不更新、下个会话生效——接受，工具输出始终权威。

## 存储布局

```
<agentDir>/pidesktop-memory/<agentId>/     # 每助手一份，跨会话
├── MEMORY.md                              # 派生索引（store 自动再生成，供人浏览）
└── topics/<id>.md                         # frontmatter: title/description/workspace?/createdAt/updatedAt + markdown 正文
```

- 主题文件为记忆单元（用户偏好 / 协作纠错 / 项目约定 / 决策理由）
- 索引由 store 派生，模型不维护（无 GA 式同步漂移）；注入用索引 = 全局主题 + 匹配当前工作区的主题，40 行安全截断
- 治理规则（静态字节，进系统提示词）：只记验证过的稳定信息；禁存易变状态/可推理常识/单次行为推断/凭证；写前先 `memory_read`、整主题替换不得丢既有要点；删除或大段改写先在对话中向用户确认，普通写入直接做、事后一句话告知；时间敏感内容标注日期；主题超过 ~15 个主动合并

## 工具（utility process customTools，不过权限门，tool-audit 自动覆盖）

- `memory_write(topic, description, content, workspace_scoped?)` —— dsh 式整主题替换（按 topic 匹配 upsert），参数即状态、在对话尾部；结果只回「已保存主题 X + 该主题索引行 + 主题总数」
- `memory_read(topic)` / `memory_list()` / `memory_search(query)`（标题命中优先，前 5 条带片段）/ `memory_delete(topic)`
- 每个 execute 先查 `settings.memory.enabled`，停用时返回提示文本（工具保持注册，开关即时生效不重建会话）

## 文件级改动

### 新建
1. **`src/main/memory-store.ts`** —— `createMemoryStore(dir, onChanged)`：`list/read/save/remove/indexMarkdown(workspace?)/search`；标题→安全文件名（拒绝路径注入）；扁平 frontmatter 解析（照 `skill-catalog.ts` 的正则模式）；损坏文件跳过自愈；tmp+rename 原子写；每次变更再生成 MEMORY.md + 触发 onChanged
2. **`src/main/runtime-memory-tools.ts`** —— `buildMemorySystemPromptBlock()`（静态治理块）、`buildMemorySnapshotBlock(snapshot)`（快照包装 + 40 行截断）、`buildMemoryTools({store, enabled, onChanged})`
3. **`src/renderer/src/MemoryPanel.tsx`** —— 浮层卡片（仿 TaskPanel）：全局/当前工作区分组列表、展开行内编辑 description+content、保存/删除、新建、总开关、空态文案。**与 TaskPanel 只读不同，这里保留人工编辑**——记忆面板是用户的治理入口（信任机制），面板写走 IPC 直改 store。钩子：`data-pane="memory-panel"`
4. **`memory-store.test.ts` / `runtime-memory-tools.test.ts`** —— vitest，tmpdir 真实文件；覆盖 upsert/索引派生/工作区过滤/损坏自愈/文件名净化/工具流（write→read→search→delete、停用返回提示）

### 修改
5. **`src/shared/protocol.ts`** —— `MemoryTopic` 类型；`RuntimeCommand` 增 `memory.update / memory.delete / memory.save`；`RuntimeMessage` 增 `{type:"memory"; memory: MemoryTopic[]}`；`DesktopSettings.memory?: {enabled}`（仿 VisionSettings）
6. **`src/main/settings.ts`** —— `normalizeMemory()` + 默认 `{enabled:true}` + migrate 挂接
7. **`src/main/resource-catalog.ts`** —— `memory?: MemoryTopic[]`（bootstrap 随 resources 下发）
8. **`src/main/index.ts`** —— `updateSettings()` 加 `case "memory.save"` 落盘（仿 vision.save）
9. **`src/main/pi-runtime.ts`**：
   - **在 `DefaultResourceLoader` 创建之前**建 memoryStore（路径只需 recordAgent.id + workspace，不依赖 sessionId）并取快照；`systemPromptOverride` 在 enabled 时拼入治理块 + 冻结快照
   - 模块镜像 `memoryStore/memoryTopics` + `emitMemory()/refreshMemory()`（照 todo 镜像模式）
   - `recordCustomTools` 拼 memoryTools、`setActiveToolsByName`、record 字段
   - `activate()` 同步镜像 + emitMemory；`disposeRecord` 清镜像；`applyMcpToolChanges` 重建数组含 memoryTools
   - `handleCommand` 增 `memory.update/delete` + `memory.save`（patch settings，仿 vision.save）
10. **`src/renderer/src/store.ts`** —— `memory` 状态 + `case "memory"` + bootstrap 初始化
11. **`src/renderer/src/App.tsx`** —— topbar 加记忆按钮 `data-control="memory-toggle"`（preview-toggle 旁）；conversation-pane 内渲染 `<MemoryPanel/>`
12. **`src/renderer/src/styles.css`** —— memory-panel 浮层样式（仿 task-panel，z-index 40，位置错开）
13. **`docs/theme-guide.md` + `AGENTS.md`** —— 登记新钩子 `memory-panel`/`memory-toggle`（迁移说明）；AGENTS.md 记忆系统约定条目（存储位置、不过权限门、快照注入 + 尾部状态流的缓存设计）；顺带把 AGENTS.md 里过时的 todo 描述（todo_create/update 等）刷新为 todo_write 现状

## 已知取舍（接受）
- 同助手多并发会话写同一主题：文件级原子、最后写赢（量级可接受）
- 面板手改后本会话快照不刷新（工具输出权威，下会话生效）
- 一期不做：复习邀请（读 session JSONL 补证据）、per-agent 开关、语义检索、跨助手共享画像

## 验证
1. `npm test`（新增测试全绿）
2. `npm run build`
3. 手动冒烟：面板增删改主题 → 模型 memory_write 后工具结果带索引行、下一轮缓存命中率不跌（看 context chip）→ 会话中途写记忆不引起全量 miss → 关开关后快照不注入、工具返回停用提示 → 切会话/切助手后记忆仍在、新会话快照更新
